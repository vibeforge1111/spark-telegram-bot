# Model-Router Cutover - Autonomous Goal (operate from this; do not wait for decisions)

GOAL: Spark's intent system works perfectly - 0 hijacks reach execution, 0 real commands silently dropped, every dispatch kernel-authorized, polished UX - by finishing the model-as-router cutover and deleting the over-engineered scaffolding. Net code must go DOWN.

SUCCESS GATE (before any live flip): on the 173-prompt corpus via ops/routeBoundaryHandlerHarness.ts with SPARK_MODEL_ROUTER=1: 0 hijack-to-action, 0 false drops on real commands, every mutation kernel-authorized; build clean; full test suite green.

AUTONOMY: Drive the whole task list (#1-13) without asking. Every decision below is PRE-MADE. Decide sensible defaults for anything unlisted and proceed. Report only at checkpoints: end of each wave, corpus validation, and immediately before the live flip.

ARCHITECTURE (decided): model routes -> deterministic kernel disposes -> untrusted input (memory/quotes/tool-output/prior turns) is DATA, never a command. Same pattern as Claude Code/Codex/Hermes. KEEP: the authority kernel (envelope -> Governor -> ledger) + the model classifier (llm.providerComplete = the SAME model as chat: codex/gpt-5.5 on the mirror, glm on Desktop). DELETE once proven: the regex decider as router, the 4 boundary detectors, the veto, the nudge, fake confidence, dead is*Request predicates.

PER-ROUTE LOOP: reuse the existing handler; keep its EXACT authority + ledger calls; add an entry to the dispatch table (src/modelDispatch.ts + the router block in src/index.ts ~line 9806); build + run tests; commit; THEN delete that route's cascade branch once it dispatches correctly. A route must exist in INTENT_PROPOSER_TAXONOMY (src/intentProposerShadow.ts) before the table can dispatch it - add it if missing. High-blast routes (modelRouter CONFIRM_ROUTES) go behind the confirm state machine.

WAVES (order): A reads - DONE: spark_wiki.answer, spawner.board; NEXT: spark.read_only_state (10 branches -> 1 entry), memory.recall, access.status. B low-blast: diagnostics.scan, memory.write, memory.delete, external_research, schedule.create, model.switch, spawner.mission_control, recursive reads. Then the pending-confirm state machine (closes the frame-resolved + confirm-echo hijack holes). C high-blast: schedule.delete, domain_chip.create, creator.mission, recursive.proposal, access.change, spawner.build LAST. Then kernel provenance + one turn_id join key. Then corpus-validate -> live flip (QA bot first, then @SparkRecursive mirror with kill-switch ready) -> DELETE scaffolding -> real-merge deploy (no dist-overlay; fix the rollback artifact; gate sync:check:strict; nightly CI on the corpus) -> final polish.

COMMIT DISCIPLINE: isolate every commit to MY files. src/index.ts and src/llm.ts carry other authors' uncommitted work (statusArgs, llm hunks) - NEVER bundle them. Stage my hunks via `git apply --cached` of a filtered patch, then `git commit` with NO pathspec. NEVER `git commit --only <paths>` (it commits working-tree content and bundles foreign hunks). Verify `git show HEAD -- src/index.ts | grep -c statusArgs` == 0. Never commit secrets/traces (.env.override + shadow logs are gitignored). No em dashes. opengrep not semgrep.

HARD GUARDRAILS (never auto): do NOT enable Level 5 / whole-computer operator (human-owned; needs the trusted-terminal approval phrase). Do NOT set SPARK_MODEL_ROUTER=1 on the live mirror until the corpus SUCCESS GATE passes. Always keep a dist rollback. STOP AND ASK only if: corpus validation shows a hijack leak I cannot close, or a migration would irreversibly change a user-visible contract with no safe default. Otherwise decide and proceed.

ARTIFACTS: branch codex/tgb-02-discovered-test-runner-20260611. Route->handler map: tasks/w668tujjs.output. Corpus: ops/natural-language-live-commands.json (73) + ops/genesis-live-telegram-100.json (100). Memory: project_spark_intent_diagnosis. Tasks #1-13 (TaskList). Kill-switch off = zero behavior change; the live mirror is untouched until the gate passes.
