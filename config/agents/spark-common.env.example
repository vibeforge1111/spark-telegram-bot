# Shared optional env for Spark agents on this machine.
# Copy to ~/.spark/config/agents/spark-common.env when you want the same
# non-secret defaults across agents. Keep real secrets out of git.

# Example:
# SPARK_X_API_BASE_URL=https://api.x.com
