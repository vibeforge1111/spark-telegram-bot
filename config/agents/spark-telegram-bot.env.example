# Optional Spark Telegram agent env.
# Copy to ~/.spark/config/agents/spark-telegram-bot.env for local installs.
# Do not commit the copied file.

# Basic X reads use the Spark agent's own bearer token.
# This is separate from domain-chip-xcontent / XContent premium credentials.
# SPARK_X_BEARER_TOKEN=
